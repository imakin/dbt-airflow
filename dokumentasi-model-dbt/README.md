Ini adalah kopi dari `../suorcecode/dbt_nyc_taxi/target` yang akan berisi file dokumentasi model dbt bila `dbt docs generate` atau pipeline airflow sudah dijalankan.

bila docker dijalankan, dokumentasi dapat diliat di http://127.0.0.1:8083